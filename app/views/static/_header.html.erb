<nav class="navbar navbar-inverse navbar-lg navbar-fixed-top mainNav" role="navigation">
  <div class="navbar-header">
    <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="navbar-collapse-03">
      <span class="sr-only">Toggle navigation</span>
    </button>
    <a class="navbar-brand" href="/">Zenvoices</a>
  </div>

  <div class="collapse navbar-collapse">
    <% if user_signed_in? %>
      <button class="btn btn-default navbar-btn btm-sm" type="button" data-target="#modal_newClient" data-toggle="modal">
        <span class="fi-plus medium"></span>
        Add Client
      </button>
    <% end %>

    <ul class="nav navbar-nav navbar-right" style="margin-right: 20px">
      <% if user_signed_in? %>
        <div class="btn-group" id="account">
          <button class="btn btn-inverse dropdown-toggle" id="account-dropdown" style="margin-top: 20px" data-target="dropdown">Account <b class="caret"></b></button>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "My Account", edit_user_registration_path %>
            <li><a href="#">Application Settings</a></li>
            <li><a href="#">Help</a></li>
            <li class="divider"></li>
            <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
          </ul>
        </div>
      <% end %>
    </ul>
  </div>
</nav>
